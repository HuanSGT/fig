#include<cstdio>
#include<string>

using namespace std;

string code,node[5][2];
char s[10000];

int main() {
    freopen("temp.mp","r",stdin);
    code = "";
    while (gets(s)) {
        code += s + "\n";
    }
    return 0;
}
